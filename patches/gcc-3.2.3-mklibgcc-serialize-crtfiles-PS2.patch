--- gcc/mklibgcc.in.orig	2018-04-22 22:18:44.762409000 -0400
+++ gcc/mklibgcc.in	2018-04-22 22:32:32.805362690 -0400
@@ -419,6 +419,9 @@ echo '	  if [ -d $$d ]; then true; else
 echo '	done'
 echo '	if [ -f stmp-dirs ]; then true; else touch stmp-dirs; fi'

+# serialize build, since they may have to generate a new c-parse.y but
+# two jobs could be started in parallel thus colliding
+prev_out=""
 for f in $EXTRA_MULTILIB_PARTS; do
   for ml in $MULTILIBS; do
     dir=`echo ${ml} | sed -e 's/;.*$//' -e 's/=/$(EQ)/g'`
@@ -433,12 +436,13 @@ for f in $EXTRA_MULTILIB_PARTS; do
     *) targ=$out ;;
     esac

-    echo $out: stmp-dirs
+    echo $out: stmp-dirs $prev_out
     echo "	$make_compile" \\
     echo '	  LIBGCC2_CFLAGS="$(LIBGCC2_CFLAGS)' $flags '" ' \\
     echo '	  MULTILIB_CFLAGS="'$flags'"' T=$t $targ

     all="$all $out"
+    prev_out="$out"
   done
 done

